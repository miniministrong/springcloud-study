spring:
  application:
    name: com.example
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
        # 修改配置中心的文件扩展名，默认为properties
        file-extension: yaml